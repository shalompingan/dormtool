function renderCategory(catId){var cat=getCategory(catId);if(!cat)return;var custom=getCustomItems(catId);var cart=state.cart;var items;if(state.priorityFilter){var week1=cat.items.filter(function(i){return i.priority==='week1';});var month1=cat.items.filter(function(i){return i.priority==='month1';});var later=cat.items.filter(function(i){return i.priority!=='week1'&&i.priority!=='month1';});items=week1.concat(month1,later);}else{items=cat.items;}var fixedHtml='';items.forEach(function(it){var isChecked=state.checked[it.id];var inCart=cart.some(function(c){return c.id===it.id;});var tierClass=getPriceTierClass(it.priceTier);var tierLabel=getPriceTierLabel(it.priceTier);var priorityHtml='';if(state.priorityFilter&&it.priority==='week1'){priorityHtml='<span class="priority-badge">Must-Have</span>';}fixedHtml+='<div class="item-card'+(isChecked?' is-checked':'')+'">'+'<div class="item-row">'+'<label class="item-check">'+'<input type="checkbox"'+(isChecked?' checked':'')+' data-iid="'+it.id+'">'+'<span class="check-box"><svg viewBox="0 0 12 12" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="2,6 5,9 10,3"/></svg></span>'+'<span class="item-name">'+it.name+'</span>'+'</label>'+'<button class="item-shop'+(inCart?' is-in-cart':'')+'" data-iid="'+it.id+'">'+(inCart ? 'Added ✓' : 'Shop ↗')+'</button>'+'</div>'+(it.desc ? '<div class="item-desc">'+it.desc+'</div>' : '')+'<div class="item-tiers">'+'<span class="price-tier '+tierClass+'">'+tierLabel+'</span>'+priorityHtml+'</div>'+'</div>';});var customHtml='';custom.forEach(function(it){var inCart=cart.some(function(c){return c.id===it.id;});customHtml+='<div class="item-card item-card--custom'+(it.checked?' is-checked':'')+'">'+'<div class="item-row">'+'<label class="item-check">'+'<input type="checkbox"'+(it.checked?' checked':'')+' data-iid="'+it.id+'">'+'<span class="check-box"><svg viewBox="0 0 12 12" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="2,6 5,9 10,3"/></svg></span>'+'<span class="item-name">'+escHtml(it.name)+'</span>'+'</label>'+'<button class="item-shop'+(inCart?' is-in-cart':'')+'" data-iid="'+it.id+'">'+(inCart ? 'Added ✓' : 'Shop ↗')+'</button>'+'<button class="item-remove" data-iid="'+it.id+'" title="Remove">✕</button>'+'</div></div>';});var addFormHtml='<div class="add-item-form" data-cat="'+catId+'">'+'<input class="add-item-input" type="text" placeholder=\'+Add your own item(e.g. "Guitar stand")\' maxlength="120">'+'<button class="add-item-btn" type="button">Add</button></div>';var filterBarHtml='<div class="filter-bar">'+'<button class="filter-btn'+(!state.priorityFilter?' is-active':'')+'" data-filter="all">All Items</button>'+'<button class="filter-btn'+(state.priorityFilter?' is-active':'')+'" data-filter="priority">Must-Haves First</button>'+'</div>';document.getElementById('categoryPanel').innerHTML=filterBarHtml+'<div class="category-header">'+'<div class="category-title">'+cat.icon+' '+cat.name+'</div>'+'<div class="cat-actions">'+'<button class="btn-select-all" data-action="select-all">✓ Select All</button>'+'<button class="btn-deselect-all" data-action="deselect-all">✕ Deselect</button>'+'</div></div>'+'<div class="items-grid">'+fixedHtml+customHtml+addFormHtml+'</div>';$$('.filter-btn',document.getElementById('categoryPanel')).forEach(function(btn){btn.addEventListener('click',function(){state.priorityFilter=this.dataset.filter==='priority';renderCategory(state.activeTab);});});$$('.item-check input[type="checkbox"]',document.getElementById('categoryPanel')).forEach(function(cb){cb.addEventListener('change',function(){toggleItem(this.dataset.iid);});});$$('.item-shop',document.getElementById('categoryPanel')).forEach(function(btn){btn.addEventListener('click',function(){toggleCart(this.dataset.iid);});});$$('.item-remove',document.getElementById('categoryPanel')).forEach(function(btn){btn.addEventListener('click',function(){removeCustomItem(catId,this.dataset.iid);});});var selBtn=$('[data-action="select-all"]',document.getElementById('categoryPanel'));var desBtn=$('[data-action="deselect-all"]',document.getElementById('categoryPanel'));if(selBtn)selBtn.addEventListener('click',function(){selectAll(catId);});if(desBtn)desBtn.addEventListener('click',function(){deselectAll(catId);});var form=$('.add-item-form[data-cat="'+catId+'"]',document.getElementById('categoryPanel'));if(form){var inp=form.querySelector('.add-item-input');var aBtn=form.querySelector('.add-item-btn');function doAdd(){var n=inp.value.trim();if(n.length<1)return;addCustomItem(catId,n);inp.value='';inp.focus();}aBtn.addEventListener('click',doAdd);inp.addEventListener('keydown',function(e){if(e.key==='Enter'){e.preventDefault();doAdd();}});}}